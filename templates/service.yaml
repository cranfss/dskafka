apiVersion: v1
kind: Service
metadata:
  name: {{ template "dskafka.fullname" . }}
  labels:
    app: {{ template "dskafka.name" . }}
    chart: {{ template "dskafka.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "dskafka.name" . }}
    release: {{ .Release.Name }}
